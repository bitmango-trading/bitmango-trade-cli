name: Build Bitmango Pro Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version Tag (e.g. 2026-05-18)'
        required: true

permissions:
  contents: write

jobs:
  pre-flight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify assets from whitelist
        run: |
          while IFS= read -r file; do
            [[ -z "$file" || "$file" =~ ^# || "$file" == ".whitelist-pro" ]] && continue
            
            # Check bitmango-pro/ subdir, then root, then scripts/ subdir
            if [ -e "bitmango-pro/$file" ]; then
                path="bitmango-pro/$file"
            elif [ -e "$file" ]; then
                path="$file"
            elif [ -e "scripts/$file" ]; then
                path="scripts/$file"
            else
              echo "Error: Required asset '$file' not found in bitmango-pro/, root, or scripts/."
              exit 1
            fi
          done < "bitmango-pro/.whitelist-pro"
          echo "All assets verified."

  build:
    needs: pre-flight
    name: Build Pro ${{ github.event.inputs.version }} - ${{ matrix.platform }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            suffix: ""
          - os: windows-latest
            platform: windows
            suffix: ".exe"
          - os: macos-latest
            platform: macos
            suffix: ""
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Install dependencies
        shell: bash
        run: |
          pip install nuitka zstandard maturin
          if [ -f "requirements.txt" ]; then pip install -r requirements.txt; fi

      - name: Build Pro Core (Rust)
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          cd bitmango-pro/bitmango-pro-core
          maturin build --release --out ../../dist_release
          cd ../../

      - name: Build bitmango
        uses: ./.github/actions/build-bitmango-binary
        with:
          entry_path: "bitmango-free/bin/bitmango"
          output_filename: "bitmango${{ matrix.suffix }}"
          platform: "${{ matrix.platform }}"

      - name: Build bitmango-vault
        uses: ./.github/actions/build-bitmango-binary
        with:
          entry_path: "bitmango-free/bin/bitmango-vault"
          output_filename: "bitmango-vault${{ matrix.suffix }}"
          platform: "${{ matrix.platform }}"

      - name: Build bitmango-help
        uses: ./.github/actions/build-bitmango-binary
        with:
          entry_path: "bitmango-free/bin/bitmango-help"
          output_filename: "bitmango-help${{ matrix.suffix }}"
          platform: "${{ matrix.platform }}"

      - name: Stage binaries
        shell: bash
        run: |
          mkdir -p bin_outputs/${{ matrix.platform }}
          mv bitmango${{ matrix.suffix }} bitmango-vault${{ matrix.suffix }} bitmango-help${{ matrix.suffix }} bin_outputs/${{ matrix.platform }}/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pro-binaries-${{ github.event.inputs.version }}-${{ matrix.platform }}
          path: bin_outputs/${{ matrix.platform }}

  collect-and-commit:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: releases-${{ github.event.inputs.version }}-pro
          submodules: false

      - name: Prepare workspace
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git reset --hard HEAD
          git clean -fd

      - name: Download all binaries
        uses: actions/download-artifact@v4
        with:
          path: dist_release/
          pattern: pro-binaries-${{ github.event.inputs.version }}-*
          merge-multiple: true

      - name: Commit and Push
        run: |
          git add dist_release/
          if ! git diff --cached --quiet; then
            git commit -m "chore: add pro binaries for release ${{ github.event.inputs.version }}"
            git pull --rebase origin releases-${{ github.event.inputs.version }}-pro
            git push origin HEAD:releases-${{ github.event.inputs.version }}-pro
          else
            echo "No changes to commit"
          fi
